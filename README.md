# goai-crisis-cloud-bridge

GOAI 2026 复赛 · 东艺危机反事实推演云端运行的桥接包（非密，全部内容为演示数据与探针脚本）。

- `run.sh`：CloudStudio 工作区只读探针（环境/Team 就绪/容器/变量计数），不写、不发、不改状态。
- `CLOUD-DISPATCH-2014-CRISIS.md`：东艺危机云端派单包（juchang-cloud-dispatch@1 + 执行契约），供 runbook 流程人工确认后在 Element 的 Leader DM 发送一次。

本仓库作为「本地 → CloudStudio 终端」的传输层，避免逐键输入长命令。
