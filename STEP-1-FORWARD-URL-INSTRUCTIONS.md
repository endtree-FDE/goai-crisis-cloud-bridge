请帮我：

在 IDE 底部面板，点「**端口**」标签（与「问题/输出/调试控制台/终端」一排，最右边）。

进入后，每个被转发的端口会有一行，类似：

```
Local Address    Forwarded Address    Visibility
3000  →         https://xxx-3000.yyy  Private
```

或者每个端口行右侧有一个「地球/外部链接」图标、或者右键「Copy forwarded address」。

**我需要**：找到端口 `13000`（agentteams-dashboard）那一行，把它显示的「**Forwarded Address / 公开地址 / Public URL**」的完整 URL 截图或直接复制贴给我（不需要隐藏的就发公开那个）。

如果 13000 行显示「**Not forwarded / 内部访问 / 未转发**」—— 同一行右边通常有一个「**+ Forward Port...**」按钮或「转发」图标，点击它，转发出来就会自动出现地址。把生成的新地址贴给我。

简短回执即可：「地址：https://……」或者「截图」。
